# Vector DB
chromadb

# 한국어 임베딩 모델
sentence-transformers

# API 호출 (The Dog API, Dog CEO API, 한국관광공사 OpenAPI)
httpx

# 공공데이터 CSV 파싱
pandas

# .env 파일 로드 (API 키 관리)
python-dotenv

# OpenAI API 클라이언트
openai

# Jupyter 노트북 커널
ipykernel

# FastAPI 백엔드
fastapi
uvicorn[standard]

# 이미지 평가 (YOLOv8 견종 분류)
ultralytics
pillow

# 비동기 SQLite (cost_tracker)
aiosqlite

# DB 연결 (AWS RDS MySQL)
pymysql
sqlalchemy[asyncio]
aiomysql

# SSH 터널링 (로컬 환경에서 RDS 접근)
sshtunnel
paramiko==3.5.1    # 4.x에서 DSSKey 제거로 sshtunnel 호환 불가
cryptography       # SSH PEM 키 파싱에 필요

# Pydantic v2 (환경변수 관리 + 유효성 검사)
pydantic-settings
pydantic[email]

# 인증 (JWT)
python-jose[cryptography]

# AWS S3 비동기 업로드
aioboto3

# APScheduler (탈퇴 Hard Delete 배치)
apscheduler

# multipart/form-data 파일 업로드
python-multipart

# 모델 학습 및 추론
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.21.0  # transformers 모델 로드/추론 시 필요
scikit-learn>=1.3.0
numpy>=1.24.0
tqdm                # 학습 진행률 표시 (transformers 의존성이지만 명시적 고정)

# docker 빌드
paramiko==2.12.0
sshtunnel==0.4.0

# 데이터 수집
beautifulsoup4